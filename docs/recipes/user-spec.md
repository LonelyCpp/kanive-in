build a "recipe" page.

i have them handwitten, i will scan them and add the images in a folder, and we can store some meta data about each in a json maybe?

the page will show a grid of them like thumbnails on a board, clicking on which opens the recipie in a modal
